<style type="text/css">
  body {
    padding-top: 60px;
    padding-bottom: 40px;
  }
  .sidebar-nav {
    padding: 9px 0;
  }

  @media (max-width: 980px) {
    /* Enable use of floated navbar text */
    .navbar-text.pull-right {
      float: none;
      padding-left: 5px;
      padding-right: 5px;
    }
  }
</style>
<div class="well sidebar-nav">
  <ul class="nav nav-list">
    <li class="nav-header">游戏管理</li>
    <li class="<%= action_name == "index" && controller_name == "materials" && 'active' %>">
      <%= link_to '我的游戏', [:custom,:materials] %>
    </li>
    <li class="<%= action_name == "index" && controller_name == "welcome" && 'active' %>">
      <%= link_to '全部游戏', [:custom,:root] %>
    </li>
  </ul>
</div>
