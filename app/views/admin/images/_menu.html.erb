<style type="text/css">
  body {
    padding-top: 60px;
    padding-bottom: 40px;
  }
  .sidebar-nav {
    padding: 9px 0;
  }

  @media (max-width: 980px) {
    /* Enable use of floated navbar text */
    .navbar-text.pull-right {
      float: none;
      padding-left: 5px;
      padding-right: 5px;
    }
  }
</style>
<div class="well sidebar-nav">
  <ul class="nav nav-list">
    <li class="nav-header">Banner</li>
    <li class="<%= controller_name == 'images' and action_name.in?(['index','edit']) && 'active' %>">
      <%= link_to '全部image', [:admin,:images] %>
    </li>
    <li class="<%= controller_name == 'categories' and action_name.eql?('new') && 'active' %>">
      <%= link_to '新建image', [:new,:admin,:image] %>
    </li>
  </ul>
</div>
